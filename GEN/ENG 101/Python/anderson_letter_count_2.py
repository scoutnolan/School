from letter_count import * # imports everything from the file including variables
word = input(str('Please enter a string ')) #input for the word used 
letter_count(word) # passes the words to the function

for i in word: # for the characters in the word, perform this
    
    print('The number of conosonants is: ', + c) # supposed to take the variables from the other file and use them here.
    print('The number of vowels is : ', + v) # did this because * after import in line one should import everything.
    print('The number of other characters is : ', + other)
